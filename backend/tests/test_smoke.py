"""Green-empty smoke test — proves the pytest harness collects and runs.

Real coverage arrives milestone by milestone (config in M1, auth in M2, the
fusion graph in M4, endpoint contracts in M7, …).
"""


def test_harness_alive():
    assert True
