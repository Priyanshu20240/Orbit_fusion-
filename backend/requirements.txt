# Orbiter Fusion Platform - Backend Dependencies
#
# M9 cleanup: dropped titiler.core, rio-tiler, mercantile, rasterio,
# xarray, scipy, Pillow, shapely, pyproj, python-multipart. They were
# the M0–M4 interim pins for the deleted titiler / getThumbURL / local
# COG path (see M5 atomic delete). The fusion pipeline is now getMapId
# + XYZ tiles + GEE; nothing in the live path imports any of these.
#
# requests + numpy are still pulled in transitively by earthengine-api
# and pystac_client; we don't pin them at top level any more (no direct
# importer left in app/).

fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic>=2.0
pydantic-settings>=2
cachetools>=5
httpx==0.26.0

# Satellite search (Sentinel-2 / Landsat 8-9) — used by /api/search/all
# and the per-platform search routes in main.py. Both are load-bearing
# in M9; the M9 plan initially listed them for removal but main.py
# still imports sentinel_service and landsat_service, so they stay.
pystac-client
planetary_computer

# Google Earth Engine for /api/fusion/gee-harmonize + refresh-mapid
earthengine-api>=0.1.370
