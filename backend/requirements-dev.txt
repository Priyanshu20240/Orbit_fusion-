# Orbiter Fusion — backend dev/test dependencies
# CI installs THIS file: `pip install -r backend/requirements-dev.txt`
-r requirements.txt

pytest>=8
pytest-cov>=5
httpx>=0.26          # FastAPI TestClient transport
